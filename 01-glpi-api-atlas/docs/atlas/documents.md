# Documentos

## Listar
- `GET /Document` com `range`.

## Criar
- `POST /Document` para metadados; upload pode exigir multipart com campo de arquivo.

## Campos
- `name`, `comment`, metadados de arquivo, vinculação a itens.