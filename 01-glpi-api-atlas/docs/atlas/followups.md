# Followups

## Listar
- `GET /ITILFollowup` com `range`.

## Criar
- `POST /ITILFollowup` com `itemtype="Ticket"`, `items_id`, `content`.

## Campos
- `itemtype`, `items_id`, `content`, `users_id`, datas de criação/modificação.