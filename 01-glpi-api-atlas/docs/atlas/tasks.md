# Tarefas

## Listar
- `GET /Task` (ou `GET /ITILTask` conforme instância) com `range`.

## Criar
- `POST /Task` com `itemtype="Ticket"`, `items_id`, `content`.

## Campos
- `itemtype`, `items_id`, `content`, `users_id`, datas e planejamento.