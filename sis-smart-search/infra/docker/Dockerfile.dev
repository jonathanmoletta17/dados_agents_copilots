FROM node:20-alpine

WORKDIR /app

# ONLY install dependencies - code comes from volume mount
# Note: Only copying package.json to ignore package-lock.json platform restrictions
COPY package.json ./
RUN npm install

EXPOSE 5173

# Code will be mounted via docker-compose volumes
CMD ["npm", "run", "dev", "--", "--host"]
